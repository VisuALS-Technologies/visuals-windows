<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VisuALS_WPF_App"
                    xmlns:eyeX="clr-namespace:EyeXFramework.Wpf;assembly=EyeXFramework">

    <Style TargetType="local:VButton" x:Key="CandyButtonStyle" BasedOn="{StaticResource BaseButtonStyle}">
        <Style.Triggers>
            <Trigger Property="Role" Value="No">
                <Setter Property="Background" Value="{StaticResource SweetCherry}"/>
            </Trigger>
            <Trigger Property="Role" Value="Stop">
                <Setter Property="Background" Value="{StaticResource SweetCherry}"/>
            </Trigger>
            <Trigger Property="Role" Value="Delete">
                <Setter Property="Background" Value="{StaticResource SweetCherry}"/>
            </Trigger>

            <Trigger Property="Role" Value="Audio">
                <Setter Property="Background" Value="{StaticResource VitaminC}"/>
            </Trigger>
            <Trigger Property="Role" Value="Speech">
                <Setter Property="Background" Value="{StaticResource VitaminC}"/>
            </Trigger>
            
            <Trigger Property="Role" Value="Language">
                <Setter Property="Background" Value="{StaticResource FreshLemon}"/>
            </Trigger>
            <Trigger Property="Role" Value="Notes">
                <Setter Property="Background" Value="{StaticResource FreshLemon}"/>
            </Trigger>
            
            <Trigger Property="Role" Value="EyeTracking">
                <Setter Property="Background" Value="{StaticResource SugarPlum}"/>
            </Trigger>

            <Trigger Property="Role" Value="Files">
                <Setter Property="Background" Value="{StaticResource BlueRaspberry}"/>
            </Trigger>
            <Trigger Property="Role" Value="Data">
                <Setter Property="Background" Value="{StaticResource BlueRaspberry}"/>
            </Trigger>
            <Trigger Property="Role" Value="Open">
                <Setter Property="Background" Value="{StaticResource BlueRaspberry}"/>
            </Trigger>
            <Trigger Property="Role" Value="Save">
                <Setter Property="Background" Value="{StaticResource BlueRaspberry}"/>
            </Trigger>

            <Trigger Property="Role" Value="Submit">
                <Setter Property="Background" Value="{StaticResource GreenApple}"/>
            </Trigger>
            <Trigger Property="Role" Value="Yes">
                <Setter Property="Background" Value="{StaticResource GreenApple}"/>
            </Trigger>
            <Trigger Property="Role" Value="New">
                <Setter Property="Background" Value="{StaticResource GreenApple}"/>
            </Trigger>
            <Trigger Property="Role" Value="Start">
                <Setter Property="Background" Value="{StaticResource GreenApple}"/>
            </Trigger>

            <Trigger Property="Role" Value="Web">
                <Setter Property="Background" Value="{StaticResource BubbleBlue}"/>
            </Trigger>
            <Trigger Property="Role" Value="Account">
                <Setter Property="Background" Value="{StaticResource BubbleBlue}"/>
            </Trigger>
            <Trigger Property="Role" Value="Display">
                <Setter Property="Background" Value="{StaticResource BubbleBlue}"/>
            </Trigger>

            <Trigger Property="Role" Value="Help">
                <Setter Property="Background" Value="{StaticResource PinkLemonade}"/>
            </Trigger>

            <Trigger Property="Role" Value="Navigation">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Arrow">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Toggle">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Settings">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Cancel">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Item">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Edit">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>
            <Trigger Property="Role" Value="Key">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>

            <Trigger Property="Role" Value="Pause">
                <Setter Property="Background" Value="{StaticResource VitaminC}"/>
            </Trigger>
            <Trigger Property="Role" Value="Home">
                <Setter Property="Background" Value="{StaticResource BubbleBlue}"/>
            </Trigger>
            <Trigger Property="Role" Value="Alarm">
                <Setter Property="Background" Value="{StaticResource SweetCherry}"/>
            </Trigger>
            <Trigger Property="Role" Value="NotSpecified">
                <Setter Property="Background" Value="{StaticResource Silver}"/>
            </Trigger>

        </Style.Triggers>
    </Style>

</ResourceDictionary>